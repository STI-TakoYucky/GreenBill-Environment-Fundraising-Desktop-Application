<UserControl x:Class="GreenBill.Components.LoadingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="LoadingControl">

    <Border Background="#80000000"
            Visibility="{Binding IsVisible, ElementName=LoadingControl}">
        <Border Background="White"
                CornerRadius="12"
                Padding="40"
                Width="400"
                Height="200"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="Gray" 
                                Opacity="0.2" 
                                ShadowDepth="4" 
                                BlurRadius="20"/>
            </Border.Effect>

            <StackPanel HorizontalAlignment="Center" 
                        VerticalAlignment="Center">
                <!-- Loading Spinner -->
                <Grid Width="60" Height="60" Margin="0,0,0,20">
                    <Ellipse Width="60" Height="60" 
                            Stroke="#E0E0E0" 
                            StrokeThickness="4"/>
                    <Ellipse Width="60" Height="60" 
                            Stroke="#00A651" 
                            StrokeThickness="4"
                            StrokeDashArray="40,100"
                            RenderTransformOrigin="0.5,0.5">
                        <Ellipse.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotate" Angle="0"/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="SpinnerRotate"
                                                       Storyboard.TargetProperty="Angle"
                                                       From="0"
                                                       To="360"
                                                       Duration="0:0:1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                </Grid>

                <TextBlock Text="{Binding LoadingText, ElementName=LoadingControl}"
                          FontSize="16"
                          FontWeight="Medium"
                          Foreground="#2C2C2C"
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Border>
</UserControl>