<UserControl x:Class="GreenBill.MVVM.View.CampaignDetailsTabs.Updates"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GreenBill.MVVM.View.CampaignDetailsTabs"
             mc:Ignorable="d" 
             Background="#FFFFFF"
             d:DesignHeight="900" d:DesignWidth="1400">

    <UserControl.Resources>
        <!-- Button Style -->
        <Style x:Key="ManageButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#4CAF50"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45a049"/>
                                <Setter Property="BorderBrush" Value="#45a049"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <!-- Header with Title and Manage Button -->
                <Grid Margin="0,0,0,20">
                    <TextBlock Text="Recent Updates" 
                           FontSize="22" 
                           FontWeight="SemiBold" 
                           Foreground="#333333"
                           VerticalAlignment="Center"/>
                    <Button Content="Manage Updates" 
                            Style="{StaticResource ManageButtonStyle}"
                            HorizontalAlignment="Right"
                            Command="{Binding GoToUpdatesPage}"
                            CommandParameter="{Binding SelectedCampaign.Id}"/>
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="UpdatesItemsControl" ItemsSource="{Binding SelectedCampaign.CampaignUpdate}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F8F9FA" 
                                        CornerRadius="8" 
                                        Padding="20"
                                        Margin="0,0,0,15">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                            <TextBlock Text="{Binding Title}" 
                                                        FontWeight="SemiBold" 
                                                        FontSize="16"
                                                        Foreground="#333333"/>
                                            <TextBlock Text=" • " 
                                                        FontSize="14" 
                                                        Foreground="#999999"
                                                        VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding CreatedAt}" 
                                                       FontSize="14" 
                                                       Foreground="#999999"
                                                       VerticalAlignment="Center"/>

                                        </StackPanel>
                                        <TextBlock Text="{Binding Description}"
                                                    FontSize="14"
                                                    TextWrapping="Wrap"
                                                    Foreground="#555555"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Update Item 1 -->
             

              
          
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>